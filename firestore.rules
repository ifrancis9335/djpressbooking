rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Default deny-all. Access is performed via server-side Admin SDK.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
