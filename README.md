# DJ Press Booking

## Local Development

Run the app with:

```bash
npm run dev
```

Permanent local base URL:

- Public: http://localhost:3000
- Admin: http://localhost:3000/admin

Set `NEXT_PUBLIC_SITE_URL=http://localhost:3000` in `.env.local` for stable customer/admin link generation.

